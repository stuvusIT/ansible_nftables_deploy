#!/usr/bin/env bash

# {{ ansible_managed }}

if ! /usr/sbin/nft -c -f /etc/nftables.conf; then
	echo "Configuration seems to be invalid"
	echo "Cancelling reload"
	exit 1
fi

exec /usr/sbin/nft -f /etc/nftables.conf
